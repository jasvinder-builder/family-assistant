[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    bench: long-running benchmarks (run with `pytest -m bench`)
    smoke: fast end-to-end checks against a running docker compose stack
filterwarnings =
    ignore::DeprecationWarning
